#include<stdio.h>
int main (){
int add (num1, num2);
int subtract(num1,num2);
{char operation;
int num1 , num2 , result;
printf("Enter an operation (+ or_):");
scanf("%d", operation);

printf("Enter two numbers(num1 or num2):");
scanf ("%d",& num1,num2);
}
return 0;
}
